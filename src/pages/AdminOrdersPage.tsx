import { useCallback, useEffect, useState } from 'react'; import AdminLayout from '../admin/AdminLayout'; import { api } from '../admin/api';
interface Order{id:number;code:string;customerName:string;customerEmail:string;total:number;status:string;createdAt:string} const statuses=['pending','confirmed','shipping','completed','cancelled'];
export default function AdminOrdersPage(){const [orders,setOrders]=useState<Order[]>([]);const [error,setError]=useState('');const load=useCallback(async()=>{try{setOrders(await api<Order[]>('/api/admin/orders'));}catch(e){setError(e instanceof Error?e.message:'Error');}},[]);useEffect(()=>{void load();},[load]);async function update(id:number,status:string){try{await api(`/api/admin/orders/${id}/status`,{method:'PUT',body:JSON.stringify({status})});await load();}catch(e){setError(e instanceof Error?e.message:'Update failed');}}return <AdminLayout title="Orders">{error&&<p className="alert alert-danger">{error}</p>}<div className="table-responsive"><table className="table table-bordered bg-white"><thead><tr><th>Code</th><th>Customer</th><th>Email</th><th>Total</th><th>Date</th><th>Status</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.code}</td><td>{o.customerName}</td><td>{o.customerEmail}</td><td>£{o.total.toFixed(2)}</td><td>{new Date(o.createdAt).toLocaleString()}</td><td><select className="form-select" value={o.status} onChange={e=>void update(o.id,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}</tbody></table></div></AdminLayout>}
